name: Run Shell Script Manually

on:
  workflow_dispatch:   # allows manual execution from GitHub UI

jobs:
  run-script:
    runs-on: self-hosted
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run shell script
        run: |
          chmod +x ./script.sh
          ./script.sh
